function createDownloadLink(){
	var svgText = document.getElementById('graph').innerHTML;
	console.log(svgText);
//	canvg('canvas',svgText);
//	if(!$("#link").length){
//		$("#footer").append('<a id="link" href="' + document.getElementById("canvas").toDataURL() + '" download="graph.png">Donload Image</a>');
//	}
}

